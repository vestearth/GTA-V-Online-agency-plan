type: object
required:
  - name
  - real_world
  - category

properties:
  name:
    type: string

  real_world:
    type: array
    items:
      type: string

  category:
    type: string
    enum: [automotive, watercraft, aircraft, motorcycle, industrial, unknown]

  description:
    type: string